import { StyleSheet } from 'react-native';

export const buttonStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  textWrapper: {
    flexDirection: 'row',
    justifyContent: 'center',
    borderRadius: 30,
    padding: 16,
  },
  text: {
    fontWeight: 'bold',
  },
});
